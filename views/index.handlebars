<br><center><h1>MVC Burgers!</h1></center><br>

<main>
  <div class="container">
    <div class="row">
    <div class="col-md-6">

	<h2>Up for grabs:</h2>	

      <h3><ol>
        {{#each burgers}} {{#unless devoured}}
        <li>
          {{burger_name}}
          <button class="change-devour btn btn-danger" data-id="{{id}}" data-newDevour="{{devour}}">
            Devour!
          </button>
        </li>
        {{/unless}} {{/each}}
      </ol></h3>
    </div>


    <div class="col-md-6">

	<h2>Already devoured:</h2>

      <h3><ol>
        {{#each burgers}} {{#if devoured}}
        <li>
          {{burger_name}}
        </li>
        {{/if}} {{/each}}
      </ol></h3>

    </div>
  </div>
</main>

<center><br><h2>Don't see one you like?</h2>
<H3>No worries, add a burger here:</H3><br></center>

<center>
<div class="row">
  <div class="col-lg-12">
    <form action="/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder=" ">
      <button type="submit">Add a New Burger</button>
    </form>
  </div>
</div>
</center>

<br>
<br>
<br>